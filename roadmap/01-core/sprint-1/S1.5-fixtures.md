# S1.5 — Reproducible NDJSON fixtures

| Field | Value |
|-------|--------|
| **Task ID** | S1.5 |
| **Status** | Todo |

---

## Description

Add reproducible NDJSON fixtures under `data/telemetry`, `data/events`, `data/ground_logs` (2–3 records each). Production-ready: reproducible fixtures (goals.md §4.5); same fixtures produce same ingest outcome.

---

## Requirements

- [ ] At least 2–3 records in `data/telemetry/` (e.g. `telemetry.ndjson`).
- [ ] At least 2–3 records in `data/events/` (e.g. `events.ndjson`).
- [ ] At least 2–3 records in `data/ground_logs/` (e.g. `ground_logs.ndjson`).
- [ ] Format and fields align with what `POST /ingest` and the agent expect (subsystem, timestamps, etc.).

---

## Checklist

- [ ] Define minimal schema (fields) for telemetry, events, ground_logs.
- [ ] Create NDJSON files with 2–3 rows each; commit under `data/`.
- [ ] Optionally add a script to generate or validate fixtures.
- [ ] Document fixture location and format in README or data/ README.

---

## Test requirements

- Ingest of fixture files via API (or direct read) succeeds; no schema errors.
- Same fixture ingested twice yields consistent result (reproducibility).
