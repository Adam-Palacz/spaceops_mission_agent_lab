# Incident run artifacts

Runtime output from **`POST /runs`** — incident payloads, escalation packets, and related JSON
(gitignored). Empty in a fresh clone; populated during local demos and tests.

Parent layout: [../README.md](../README.md).
