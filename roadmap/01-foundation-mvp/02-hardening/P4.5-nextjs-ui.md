# P4.5 — Optional Next.js UI

| Field | Value |
|-------|--------|
| **Task ID** | P4.5 |
| **Status** | Done |

---

## Description

Optional Next.js app: incident list, approval request list, Approve/Reject buttons with auth (same as API key/token as backend). Improves operator experience; not required for MVP but listed in roadmap Phase 4.

---

## Requirements

- [x] Next.js app under `apps/ui/`; can run locally and call FastAPI.
- [x] Page includes incidents list (`GET /runs`), approval list (`GET /approvals`), and Approve/Reject buttons calling backend endpoints.
- [x] Auth: passes API key (`X-API-Key`) to FastAPI approval endpoints.
- [x] Read-only incident view + functional approval flow.
- [x] Documented run instructions and env vars (`NEXT_PUBLIC_API_BASE_URL`, `NEXT_PUBLIC_APPROVAL_API_KEY`).
- [x] UI + API containerization path for local ops (`apps/ui/Dockerfile`, `apps/api/Dockerfile`, compose `ui` profile).

---

## Checklist

- [x] Scaffolded Next.js app + env config.
- [x] Added incidents list page using new `GET /runs` API endpoint.
- [x] Added approvals list + Approve/Reject actions with API key.
- [x] Added loading/error handling for both lists and action calls.
- [x] Added docs (`apps/ui/README.md`) + docs index link.
- [x] Added Docker packaging (`Dockerfile`, `.dockerignore`) and compose profile (`ui`).
- [x] Task completed and synchronized with board.

---

## Test requirements

- [x] UI loads; incidents and approvals can load from API when configured.
- [x] Clicking Approve/Reject calls API and refreshes approvals.
- [x] Invalid/missing API key surfaces API errors in UI.
