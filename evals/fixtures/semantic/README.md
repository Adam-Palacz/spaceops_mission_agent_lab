# Semantic eval fixtures

One JSON file per semantic eval case (citation precision, escalation reasons, tool empty vs failure).
Consumed by `python -m evals.semantic` / `make semantic-check`.

Case definitions: [../../semantic_cases.yaml](../../semantic_cases.yaml).
