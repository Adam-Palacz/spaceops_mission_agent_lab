[pytest]
# pytest-asyncio: set default loop scope explicitly to avoid PytestDeprecationWarning
# and future change of default (function scope is the intended future default).
asyncio_default_fixture_loop_scope = function
