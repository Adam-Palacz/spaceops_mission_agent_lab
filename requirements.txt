# SpaceOps Mission Agent Lab — pinned deps (Python 3.12)
# Install: pip install -r requirements.txt
# Production-ready: exact versions for reproducible installs (goals.md §4.5)

# API
fastapi==0.135.1
uvicorn[standard]==0.41.0
prometheus-client==0.24.1

# Agent / LangGraph
langgraph==1.1.2
langchain-core==0.3.21
langchain-openai==1.1.11

# Postgres + pgvector (RAG, KB)
psycopg2-binary==2.9.10
pgvector==0.3.6

# OTel (traces — S1.10)
opentelemetry-api==1.28.0
opentelemetry-sdk==1.28.0
opentelemetry-exporter-otlp-proto-grpc==1.28.0
opentelemetry-instrumentation-fastapi==0.61b0

# HTTP client, config
httpx==0.27.2
pydantic==2.11.0
pydantic-settings==2.13.1
python-dotenv==1.2.2

# Data (fixtures, optional analytics)
duckdb==1.1.3

# Evals (S1.11 — cases YAML)
PyYAML==6.0.2

# MCP (S1.6 — Telemetry + KB servers)
mcp[cli]==1.26.0

# Dev / CI (lint, type-check, test)
ruff==0.8.4
mypy==1.19.1
pytest==8.3.4
pytest-asyncio==1.3.0
