# S1.13 — pre-commit, GitHub Actions, README

| Field | Value |
|-------|--------|
| **Task ID** | S1.13 |
| **Status** | Todo |

---

## Description

Add pre-commit hooks (ruff, mypy), GitHub Actions workflow (lint, pytest, evals), and root README with how to run, how to call ingest, how to run tests. Goals: production-ready single command; NF7 (documentation).

---

## Requirements

- [ ] pre-commit config: ruff (lint), mypy (type check); run on commit or manually.
- [ ] GitHub Actions: checkout, install deps, ruff, mypy, pytest (runs **tests/** from S1.14), evals (from S1.11).
- [ ] Root README: how to run stack (e.g. docker-compose, make run), how to call POST /ingest, how to run tests and evals locally.
- [ ] README points to goals.md, project_doc.md, roadmap_F1.md, docs/ (and roadmap/ if desired).

---

## Checklist

- [ ] Add `.pre-commit-config.yaml` with ruff and mypy; document `pre-commit install`.
- [ ] Add `.github/workflows/ci.yml` (or similar): install from lockfile, run ruff, mypy, pytest tests/, evals.
- [ ] Update root README: Quick start (run stack, ingest, trigger run), Testing, Links to docs.
- [ ] Ensure CI uses same Python version and deps as local (lockfile from S1.3).

---

## Test requirements

- `pre-commit run --all-files` passes on clean repo (or documents known mypy/ruff exceptions).
- Push triggers CI; workflow runs and passes when code and evals are green.
- New contributor can follow README to run stack and run tests.
