# S2.3 — Decide (safe/restricted) + Act

| Field | Value |
|-------|--------|
| **Task ID** | S2.3 |
| **Status** | Done |

---

## Description

Extend Decide node: output steps tagged **safe** vs **restricted**. Act node: execute safe steps (call Ticketing or GitOps MCP); for restricted steps, call OPA (S2.4) and if allow create approval request (S2.5). Goals: F4, F5, F6.

---

## Requirements

- [x] Decide output: list of steps; each step has type (safe | restricted) and action (e.g. create_ticket, create_pr, change_config).
- [x] Act node: for each safe step, call corresponding MCP (Ticketing or GitOps); for restricted, do not execute directly — call OPA then create approval request if allowed.
- [x] Safe steps execute in same run; restricted steps only after human approval (S2.6).
- [x] Citation grounding (NF5a) still applies to plan steps.

---

## Checklist

- [x] Extend Decide prompt/schema: add `safe` and `action_type` (create_ticket, create_pr, change_config, report) to each step.
- [x] Act node: loop over steps; if safe and action_type create_ticket/create_pr → invoke MCP; if restricted → call OPA (stub in S2.4), if allow → append to approval_requests; on deny → escalate with policy_deny.
- [x] Handle OPA deny/error in Act (deny + escalation; S2.4 stub returns False).
- [x] Audit log entry per action (create_ticket, create_pr, approval_request, opa_check).

---

## Test requirements

- [x] Run with plan containing safe steps: ticket and/or PR created when Ticketing/GitOps MCP are running.
- [x] Run with plan containing restricted step: no direct execution; approval request appended when OPA allows (S2.4 will wire real OPA).
- [x] OPA deny (stub): no approval request; escalation with reason policy_deny and audit entry.
