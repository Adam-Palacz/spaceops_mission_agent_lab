# S2.2 — MCP Ticketing + MCP GitOps

| Field | Value |
|-------|--------|
| **Task ID** | S2.2 |
| **Status** | Done |

---

## Description

Implement MCP Ticketing: `create_ticket(title, body)` with mock implementation (append-only NDJSON file). Implement MCP GitOps: `create_pr(repo_path, branch, files)` that prepares config changes under `ops-config/` (subtree from S2.1) so Act can propose GitOps PRs. Goals: F5 (Act safe), roadmap S2 outcomes. Git branch/PR creation against a remote can be layered on top (CI or later iteration) using the same file layout.

---

## Requirements

- [x] Ticketing MCP: tool `create_ticket(title, body)`; mock stores to file (`data/incidents/tickets.ndjson`).
- [x] GitOps MCP: tool `create_pr(repo_path, branch, files)`; writes/updates files under `ops-config/` (from S2.1), returning a summary for later PR creation.
- [ ] Agent can call both from Act node (wired in S2.3).
- [x] GitOps uses ops-config from S2.1; can later be wired to a GitHub-backed remote via env (e.g. `OPS_CONFIG_PATH`, `GITHUB_TOKEN`).

---

## Checklist

- [x] Create MCP Ticketing server; implement `create_ticket`; choose storage (append-only NDJSON file).
- [x] Create MCP GitOps server; implement `create_pr` that writes files under ops-config/ based on `files` specs.
- [ ] Add MCP tools to agent tool list; ensure Act node can invoke them (S2.3).
- [ ] Document env vars for future remote PR integration (e.g. `OPS_CONFIG_PATH`, `GITHUB_TOKEN`, GitHub repo slug).

---

## Test requirements

- [x] `create_ticket("Test", "Body")` persists and is visible in `data/incidents/tickets.ndjson`.
- [x] `create_pr(ops-config, "agent/incident-123", [{"path": "alerts/foo.yaml", "content": \"...\"}])` writes `ops-config/alerts/foo.yaml` with the given content and returns a summary including `branch` and `files`.
