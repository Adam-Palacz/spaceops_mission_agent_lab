# P4.3 — Runbook: How to add an eval case

| Field | Value |
|-------|--------|
| **Task ID** | P4.3 |
| **Status** | Done |

---

## Description

Write a runbook: how to add an eval case (cases.yaml format, scoring.py expectations, CI behaviour). Required for NF7 and docs/README Maintenance; supports post-incident loop (P4.7).

---

## Requirements

- [x] Runbook exists (e.g. `docs/runbooks/add_eval_case.md`).
- [x] Describes: case format (input, expected_subsystem, expected_citations, must_escalate, etc.), where to add it (evals/cases.yaml or similar), how scoring.py uses it, and that CI runs evals.
- [x] Optional: how to add injection case; how to add "must escalate" case.
- [x] New contributor or AI can add a case and see it run in CI.

---

## Checklist

- [x] Create runbook under docs/ (e.g. docs/runbooks/add_eval_case.md).
- [x] Document cases.yaml (or equivalent) schema and examples.
- [x] Document scoring.py: how it loads cases, runs agent, compares output, computes score.
- [x] Document CI: which job runs evals; how to run locally.
- [x] Link from docs/README.md and from P4.7 (post-incident: add case per incident).

---

## Test requirements

- [x] Following the runbook, add a new case; run evals locally and in CI; case is executed and score reflects it.
- [x] docs/README or Maintenance section references this runbook.
