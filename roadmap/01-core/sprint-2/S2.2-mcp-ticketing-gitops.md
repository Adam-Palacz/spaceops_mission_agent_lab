# S2.2 — MCP Ticketing + MCP GitOps

| Field | Value |
|-------|--------|
| **Task ID** | S2.2 |
| **Status** | Todo |

---

## Description

Implement MCP Ticketing: `create_ticket(title, body)` with mock implementation (e.g. append to file or DB). Implement MCP GitOps: `create_pr(repo_path, branch, files)` that opens a real PR to ops-config (GitHub API or git CLI). Goals: F5 (Act safe), roadmap S2 outcomes.

---

## Requirements

- [ ] Ticketing MCP: tool `create_ticket(title, body)`; mock stores to file (e.g. `data/incidents/tickets.json`) or DB table.
- [ ] GitOps MCP: tool `create_pr(repo_path, branch, files)`; creates branch, commits files, opens PR to ops-config.
- [ ] Agent can call both from Act node (wired in S2.3).
- [ ] GitOps uses ops-config from S2.1; auth via env (e.g. GitHub token).

---

## Checklist

- [ ] Create MCP Ticketing server; implement `create_ticket`; choose storage (file or DB).
- [ ] Create MCP GitOps server; implement `create_pr` (GitHub API or `git` + push + GitHub API for PR).
- [ ] Add MCP tools to agent tool list; ensure Act node can invoke them.
- [ ] Document env vars (e.g. GITHUB_TOKEN, OPS_CONFIG_REPO).

---

## Test requirements

- [ ] `create_ticket("Test", "Body")` persists and is visible in storage.
- [ ] `create_pr(ops-config, "agent/incident-123", [{"path": "alerts/foo.yaml", "content": "..."}])` opens a PR; branch and files are correct.
