# Eval fixtures

Static JSON inputs for **deterministic** eval paths (no live LLM). Used by semantic scoring and
related tests.

| Subfolder | Purpose |
|-----------|---------|
| **semantic/** | Per-case agent report fixtures for `python -m evals.semantic`. |

Cases: `evals/semantic_cases.yaml`. Overview: [../README.md](../README.md).
