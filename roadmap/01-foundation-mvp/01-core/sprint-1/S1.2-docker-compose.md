# S1.2 — Docker Compose (Postgres, OTel, Jaeger)

| Field | Value |
|-------|--------|
| **Task ID** | S1.2 |
| **Status** | Done |

---

## Description

Add `infra/docker-compose.yml` with Postgres 15+ and pgvector extension, OTel Collector, and Jaeger. One command (e.g. `docker-compose up -d`) brings up everything needed for local run. Production-ready criterion: single-command local run (goals.md §4.5).

---

## Requirements

- [x] Postgres 15+ with pgvector extension available.
- [x] OTel Collector and Jaeger services defined; agent/API will export to them later (S1.10).
- [x] Single command starts all (e.g. `docker compose -f infra/docker-compose.yml up -d`).
- [ ] Document in root README how to run (can be in S1.13).

---

## Checklist

- [x] Create `infra/docker-compose.yml`.
- [x] Add service: postgres, image with pgvector (e.g. `pgvector/pgvector:pg15`).
- [x] Add service: otel-collector (config in infra or default).
- [x] Add service: jaeger (all-in-one or collector + query + UI).
- [x] Wire OTel Collector to export to Jaeger if needed.
- [x] Test: `docker compose up -d` then check containers healthy.

---

## Test requirements

- `docker-compose up -d` (from repo root or infra/) succeeds.
- Postgres accepts connections and has pgvector; Jaeger UI is reachable (e.g. http://localhost:16686).
