# S1.7 — LangGraph: Triage → Investigate → Decide → Report

| Field | Value |
|-------|--------|
| **Task ID** | S1.7 |
| **Status** | Todo |

---

## Description

Implement LangGraph pipeline in `apps/agent`: Triage (subsystem + risk) → Investigate (call Telemetry + KB MCPs, collect citations) → Decide (plan where each step must reference doc_id or snippet — NF5a) → Report (summary, evidence, actions, rollback, trace URL). No Act node yet. Goals: F2, F3, F4, F7; NF5a citation grounding.

---

## Requirements

- [ ] Triage node: classify subsystem (ADCS/Power/Thermal/Comms/Payload/Ground) and risk; write incident record.
- [ ] Investigate node: call Telemetry MCP and KB MCP; attach hypotheses and citations to state.
- [ ] Decide node: produce plan (list of steps); each step must reference at least one doc_id or telemetry/snippet ID (citation grounding).
- [ ] Report node: generate summary, evidence (snippets + citations), proposed actions, rollback, trace link (Jaeger URL).
- [ ] State schema: incident_id, subsystem, risk, hypotheses, citations, plan, report.
- [ ] Pipeline invocable from API/CLI trigger (S1.4).

---

## Checklist

- [ ] Define state schema (e.g. TypedDict or Pydantic).
- [ ] Implement Triage node (LLM or rule-based); persist incident record.
- [ ] Implement Investigate node: call MCPs, aggregate citations.
- [ ] Implement Decide node: LLM outputs plan; validate each step has doc_id/snippet ref.
- [ ] Implement Report node: format summary, evidence, actions, rollback, trace link.
- [ ] Wire graph: Triage → Investigate → Decide → Report; connect to run trigger.

---

## Test requirements

- End-to-end run on fixture incident produces report with trace link.
- Plan steps in output contain at least one citation (doc_id or snippet id).
- Triage output includes subsystem and risk; Investigate output includes hypotheses + citations.
