{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "spaceops.fullname" . }}-api
  namespace: {{ include "spaceops.namespace" . }}
  labels:
    {{- include "spaceops.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - name: http
      port: {{ .Values.api.port }}
      targetPort: http
  selector:
    {{- include "spaceops.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "spaceops.fullname" . }}-api
  namespace: {{ include "spaceops.namespace" . }}
  labels:
    {{- include "spaceops.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      {{- include "spaceops.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        {{- include "spaceops.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: api
    spec:
      serviceAccountName: {{ include "spaceops.componentServiceAccountName" (dict "root" . "component" "api") }}
      containers:
        - name: api
          image: {{ include "spaceops.image" (dict "repository" .Values.images.api.repository "tag" .Values.images.api.tag) }}
          imagePullPolicy: {{ .Values.images.api.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.api.port }}
          env:
            {{- include "spaceops.apiEnv" . | nindent 12 }}
            {{- include "spaceops.secretEnvOptional" (list . "OPENAI_API_KEY" "OPENAI_API_KEY") | nindent 12 }}
            {{- include "spaceops.secretEnvOptional" (list . "APPROVAL_API_KEY" "APPROVAL_API_KEY") | nindent 12 }}
            {{- include "spaceops.secretEnvOptional" (list . "GITHUB_TOKEN" "GITHUB_TOKEN") | nindent 12 }}
            {{- include "spaceops.secretEnvOptional" (list . "CURSOR_SH_API_KEY" "CURSOR_SH_API_KEY") | nindent 12 }}
            {{- include "spaceops.secretEnvOptional" (list . "GPU_LLM_API_KEY" "GPU_LLM_API_KEY") | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.api.persistence.data.enabled }}
            - name: data
              mountPath: /app/data
            {{- end }}
            {{- if .Values.api.persistence.opsConfig.enabled }}
            - name: ops-config
              mountPath: /app/ops-config
            {{- end }}
            {{- if .Values.api.persistence.var.enabled }}
            - name: var
              mountPath: /app/var
            {{- end }}
      volumes:
        {{- if .Values.api.persistence.data.enabled }}
        - name: data
          {{- if .Values.api.persistence.data.emptyDir }}
          emptyDir: {}
          {{- end }}
        {{- end }}
        {{- if .Values.api.persistence.opsConfig.enabled }}
        {{- if .Values.api.persistence.opsConfig.configMapName }}
        - name: ops-config
          configMap:
            name: {{ .Values.api.persistence.opsConfig.configMapName }}
        {{- else if .Values.api.persistence.opsConfig.emptyDir }}
        - name: ops-config
          emptyDir: {}
        {{- end }}
        {{- end }}
        {{- if .Values.api.persistence.var.enabled }}
        - name: var
          {{- if .Values.api.persistence.var.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.api.persistence.var.existingClaim }}
          {{- else if .Values.api.persistence.var.emptyDir }}
          emptyDir: {}
          {{- end }}
        {{- end }}
{{- end }}
