# P4.2 — Runbook: How to add a new MCP

| Field | Value |
|-------|--------|
| **Task ID** | P4.2 |
| **Status** | Done |

---

## Description

Write a runbook that explains how to add a new MCP server: template or minimal server, registration (where the agent discovers tools), and wiring in the agent (LangGraph tool list). Required for NF7 and docs/README Maintenance.

---

## Requirements

- [x] Runbook exists (e.g. `docs/runbooks/add_new_mcp.md` or section in architecture).
- [x] Steps: create MCP server (template/language), define tools, register with agent (or tool registry), add to agent graph so it can be called.
- [x] Optional: link to MCP spec or example (Telemetry/KB) in repo.
- [x] AI and humans can follow it to add a new MCP without guessing.

---

## Checklist

- [x] Create runbook file under docs/ (e.g. docs/runbooks/add_new_mcp.md).
- [x] Document: 1) create server (folder, entrypoint, tools), 2) expose via MCP protocol, 3) register in agent (config or code), 4) add to Act/Investigate if needed.
- [x] Add link from docs/README.md or roadmap Maintenance.
- [ ] Optionally add checklist: update BOARD if new MCP is a roadmap task.

---

## Test requirements

- [x] Someone following the runbook can add a stub MCP and see it callable from the agent (or from tests).
- [x] docs/README.md references this runbook where appropriate.
