# S1.3 — Pinned deps + lockfile

| Field | Value |
|-------|--------|
| **Task ID** | S1.3 |
| **Status** | Done |

---

## Description

Pin dependencies and add a lockfile so that installs are reproducible. Production-ready criterion (goals.md §4.5): pinned deps + lockfile; no floating deps in CI.

---

## Requirements

- [x] Dependencies listed with versions (e.g. `requirements.txt` with `package==x.y.z` or use poetry/pipenv).
- [x] Lockfile present (e.g. `poetry.lock`, `pipenv.lock`, or `requirements.txt` with exact versions).
- [ ] CI (S1.13) uses the same lockfile/requirements for install.

---

## Checklist

- [x] Choose tooling: plain pip + requirements.txt, or poetry, or pipenv.
- [x] Add main dependencies (fastapi, uvicorn, langgraph, postgres client, etc.) with versions.
- [x] Generate lockfile or freeze; commit it.
- [x] Document in README how to install (can be S1.13).

---

## Test requirements

- Fresh clone + install from lockfile/requirements yields same versions.
- `pip install -r requirements.txt` (or `poetry install`) runs without errors.
