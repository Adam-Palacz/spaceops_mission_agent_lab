# P4.2 — Runbook: How to add a new MCP

| Field | Value |
|-------|--------|
| **Task ID** | P4.2 |
| **Status** | Todo |

---

## Description

Write a runbook that explains how to add a new MCP server: template or minimal server, registration (where the agent discovers tools), and wiring in the agent (LangGraph tool list). Required for NF7 and docs/README Maintenance.

---

## Requirements

- [ ] Runbook exists (e.g. `docs/runbooks/add-new-mcp.md` or section in architecture).
- [ ] Steps: create MCP server (template/language), define tools, register with agent (or tool registry), add to agent graph so it can be called.
- [ ] Optional: link to MCP spec or example (Telemetry/KB) in repo.
- [ ] AI and humans can follow it to add a new MCP without guessing.

---

## Checklist

- [ ] Create runbook file under docs/ (e.g. docs/runbooks/add-new-mcp.md).
- [ ] Document: 1) create server (folder, entrypoint, tools), 2) expose via MCP protocol, 3) register in agent (config or code), 4) add to Act/Investigate if needed.
- [ ] Add link from docs/README.md or roadmap Maintenance.
- [ ] Optionally add checklist: update BOARD if new MCP is a roadmap task.

---

## Test requirements

- Someone following the runbook can add a stub MCP and see it callable from the agent (or from tests).
- docs/README.md or docs/diagrams.md references this runbook where appropriate.
