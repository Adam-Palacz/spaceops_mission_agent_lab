# S1.1 — Create directory structure

| Field | Value |
|-------|--------|
| **Task ID** | S1.1 |
| **Status** | Done |

---

## Description

Create the full directory structure for the project so that apps, data, KB, evals, infra, and docs have a clear place. Align with repo structure in [docs/architecture/repo_structure.mmd](../../../docs/architecture/repo_structure.mmd) and project_doc.md §6.

---

## Requirements

- [x] `apps/api`, `apps/agent`, `apps/mcp/telemetry_server`, `apps/mcp/kb_server` exist.
- [x] `data/` (or subdirs `telemetry`, `events`, `ground_logs`, `incidents` as needed).
- [x] `kb/runbooks`, `kb/postmortems` (and optionally `kb/policies`).
- [x] `evals`, `infra`, `docs` exist.
- [x] No orphan files; empty dirs can use a `.gitkeep` if you want to commit them.

---

## Checklist

- [x] Create `apps/` and subdirs (api, agent, mcp/telemetry_server, mcp/kb_server).
- [x] Create `data/` (and subdirs if desired).
- [x] Create `kb/runbooks`, `kb/postmortems`.
- [x] Create `evals`, `infra`, `docs` (docs may already exist).
- [x] Verify with `find` or file explorer; update BOARD when done.

---

## Test requirements

- Repo root contains exactly the expected top-level dirs.
- No typos in path names (e.g. `telemetry_server` not `telemetery_server`).
