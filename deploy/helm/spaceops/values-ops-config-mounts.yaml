# Generated by scripts/render_ops_config_kustomize.py — do not edit by hand.
# ConfigMap keys must be flat (no '/'); Helm maps keys -> subpaths under /app/ops-config.
api:
  persistence:
    opsConfig:
      configMapItems:
        - key: alerts.agent-proposed.yaml
          path: alerts/agent-proposed.yaml
        - key: alerts.approved-change.yaml
          path: alerts/approved-change.yaml
        - key: alerts.test_threshold.yaml
          path: alerts/test_threshold.yaml
        - key: alerts.thresholds.yaml
          path: alerts/thresholds.yaml
        - key: channels.channel_list.yaml
          path: channels/channel_list.yaml
