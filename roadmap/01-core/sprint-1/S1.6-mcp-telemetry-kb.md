# S1.6 — MCP Telemetry + MCP KB (RAG)

| Field | Value |
|-------|--------|
| **Task ID** | S1.6 |
| **Status** | Done |

---

## Description

Implement MCP Telemetry server: `query_telemetry(time_range, channels)` (read from data/ or DuckDB). Implement MCP KB server: `search_runbooks(query)`, `search_postmortems(signature)` with RAG over pgvector. Index 2–3 runbooks and 1 postmortem under `kb/runbooks`, `kb/postmortems`. Goals: F3 (Investigate), roadmap S1 outcomes.

---

## Requirements

- [x] Telemetry MCP: tool `query_telemetry(time_range, channels)`; returns data from fixtures or DuckDB.
- [x] KB MCP: tools `search_runbooks(query)`, `search_postmortems(signature)`; return chunks + doc IDs (citations).
- [x] RAG: Postgres + pgvector; embedding of runbooks and postmortems; index 2–3 runbooks, 1 postmortem.
- [ ] Agent can call these MCPs (wiring in S1.7).

---

## Checklist

- [x] Create MCP Telemetry server (Python or TS); implement `query_telemetry`; read from data/ or DuckDB.
- [x] Create MCP KB server; connect to Postgres/pgvector.
- [x] Script or step to embed `kb/runbooks/*`, `kb/postmortems/*` and store in pgvector.
- [x] Add 2–3 sample runbook files and 1 postmortem in kb/.
- [x] Expose tools via MCP protocol so LangGraph can call them.

---

## Test requirements

- `query_telemetry` with valid time range returns non-empty result when fixture data overlaps.
- `search_runbooks("power")` (or similar) returns at least one chunk with doc_id; same for postmortems.
- MCP servers start and respond to tool calls (manual or unit test).
