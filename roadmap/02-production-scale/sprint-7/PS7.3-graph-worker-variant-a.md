# PS7.3 — Graph worker Variant A

| **Task ID** | PS7.3 | **Status** | Todo |

Separate `agentWorker` Deployment + queue; kill worker mid-run → resume without breaking idempotency (PS3.2). ADR update for 0003/0005.
