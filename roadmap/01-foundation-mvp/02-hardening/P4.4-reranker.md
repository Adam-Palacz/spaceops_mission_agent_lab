# P4.4 — Reranker for RAG (NF5)

| Field | Value |
|-------|--------|
| **Task ID** | P4.4 |
| **Status** | Todo |

---

## Description

Add a reranker (local cross-encoder or API) for KB search results to improve citation quality. NF5 (Should). If already implemented in S1/S2, mark Done and document; otherwise implement in Phase 4.

---

## Requirements

- [ ] After retrieval from pgvector (or hybrid), rerank the top-k chunks with a reranker model.
- [ ] Reranker: local (e.g. cross-encoder) or external API; configurable.
- [ ] MCP KB or agent uses reranked list for citations (top-n after rerank).
- [ ] Document model/API and config (env or config file).
- [ ] Evals (citation precision, MoE2) can benefit; optional to add reranker-specific eval.

---

## Checklist

- [ ] Choose reranker: local (sentence-transformers cross-encoder) or API (e.g. Cohere rerank).
- [ ] Integrate in KB search path: retrieve → rerank → return top-n to agent.
- [ ] Add config (model name or API URL, top_n); document in README or runbook.
- [ ] Run existing evals; ensure citation quality not regressed (optional: add citation precision metric).
- [ ] If already done in S1/S2: update this task file to "Done" and add one-line note where it lives.

---

## Test requirements

- KB search with reranker returns different (better) order than without when tested on sample queries.
- No regression in existing evals (triage, citation presence).
- Citation precision (MoE2) improves or stays same when measured (if measured).
