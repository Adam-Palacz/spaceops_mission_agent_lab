# P4.4 — Reranker for RAG (NF5)

| Field | Value |
|-------|--------|
| **Task ID** | P4.4 |
| **Status** | Done |

---

## Description

Add a reranker (local cross-encoder or API) for KB search results to improve citation quality. NF5 (Should). If already implemented in S1/S2, mark Done and document; otherwise implement in Phase 4.

---

## Requirements

- [x] After retrieval from pgvector, rerank the top-k chunks with an optional reranker.
- [x] Reranker: local (dependency-free lexical) or external API; configurable.
- [x] MCP KB uses reranked list for citations (top-n after rerank).
- [x] Document model/API and config (env or config file).
- [x] Evals can benefit; reranker-specific eval is optional (not added in MVP).

---

## Checklist

- [x] Choose reranker: local lexical reranker (dependency-free) with optional LLM mode.
- [x] Integrate in KB search path: retrieve → rerank → return top-n to agent.
- [x] Add config (mode + candidate pool size); document in KB server README.
- [x] Run existing evals; ensure citation quality not regressed (optional: reranker-specific metric).
- [ ] If already done in S1/S2: update this task file to "Done" and add one-line note where it lives.

---

## Test requirements

- [x] Reranker returns a different (deterministically re-ordered) output for relevant chunks.
- [x] No regression in existing evals (triage, citation presence).
- [ ] Citation precision (MoE2) improves or stays same when measured (if measured).
