# P4.6 — Expand eval suite (20–30 cases)

| Field | Value |
|-------|--------|
| **Task ID** | P4.6 |
| **Status** | Done |

---

## Description

Expand the eval suite to roughly 20–30 cases; add citation precision (or MoE) into scoring where applicable. Reduces regressions and supports MoE1–MoE4 (goals.md §4.4). Roadmap: "Expand eval suite (e.g. 20–30 cases); citation precision / MoE in scoring."

---

## Requirements

- [x] At least 20–30 eval cases total (triage, citation, escalation, injection).
- [x] Citation precision (MoE2): evals flag when plan steps are not supported by citations.
- [x] Triage accuracy (MoE1), unsafe-action rate = 0 (MoE3), escalation correctness (MoE4) remain covered in scoring logic.
- [x] CI runs full suite via `python -m evals.scoring`; required checks remain documented.
- [x] Cases remain deterministic (pinned model/temperature path, stable schema and expectations).

---

## Checklist

- [x] Added standard cases to reach 20 (`evals/cases.yaml`) with broader triage, citation, and escalation coverage.
- [x] Extended scoring for citation precision (`require_citation_precision` in `evals/scoring.py`).
- [x] Updated runbook (`docs/runbooks/add_eval_case.md`) for the new case field and scoring behavior.
- [x] CI command remains `python -m evals.scoring` with full-suite pass gate.
- [x] Existing MoE checks preserved while expanding coverage.

---

## Test requirements

- [x] `evals/cases.yaml` now contains 20 standard cases.
- [x] Citation precision is computed and can fail unsupported step citations.
- [x] Unit tests for eval scoring pass (`tests/test_evals.py`).
