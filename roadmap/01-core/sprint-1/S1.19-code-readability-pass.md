# S1.19 — (Optional) Code readability / maintainability pass

| Field | Value |
|-------|--------|
| **Task ID** | S1.19 |
| **Status** | Todo |
| **Priority** | Nice-to-have; not a blocker for S2 |

---

## Description

Several files use a very compressed style (many statements in one line, dense conditionals). This will make code review and debugging harder in S2 when approvals, OPA, and Act are added. This task is a light pass to improve readability without large refactors.

---

## Requirements

- [ ] **Target areas:** Agent nodes, MCP client, evals scoring, config — split long one-liners into named steps or multiple lines where it clarifies intent; avoid changing behaviour.
- [ ] **Do not:** Full refactor or new abstractions; only local clarity (variable names, line breaks, simple extra comments where logic is non-obvious).
- [ ] Optional: add a short "Code style" note in README or docs (e.g. prefer one statement per line for conditionals; name complex expressions).

---

## Checklist

- [ ] Review `apps/agent/nodes.py`, `apps/agent/mcp_client.py`, `evals/scoring.py` for one-liners that hurt readability; break or name where useful.
- [ ] Ensure tests and CI still pass after edits.
- [ ] (Optional) Document a one-paragraph style preference in README or docs/README.md.

---

## Test requirements

- [ ] `pytest tests/` passes after readability edits; no behaviour change.
- [ ] `python -m evals.scoring` passes (or evals not affected by changed files).
- [ ] CI (lint, mypy, pytest, evals) green.
- [ ] Optional: short code review or self-check that key conditionals in nodes/graph/scoring are easier to follow.
