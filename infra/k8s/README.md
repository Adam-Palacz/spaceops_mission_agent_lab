# Kubernetes infra assets

Kubernetes-related files that are **not** the Helm chart (see `deploy/helm/spaceops/`).

| Subfolder | Purpose |
|-----------|---------|
| **local/** | kind cluster config and notes for `make k8s-up`. |

Runbook: [docs/runbooks/local_k8s_dev.md](../../docs/runbooks/local_k8s_dev.md).
