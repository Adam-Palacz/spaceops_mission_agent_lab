# S2.1 — ops-config repo / subtree

| Field | Value |
|-------|--------|
| **Task ID** | S2.1 |
| **Status** | Done |

---

## Description

Create or clone an `ops-config` repo (or subtree in this repo) with sample config (e.g. alert thresholds YAML). This is the target for GitOps PRs from the agent (S2.2, S2.6). Implemented as a **subtree** (folder `ops-config/` at repo root), logically separable so it can be split to a separate repo later if desired.

---

## Requirements

- [x] ops-config exists and is reachable (subtree under repo root).
- [x] Contains at least one sample config file (e.g. `alerts/thresholds.yaml` or similar).
- [x] GitOps MCP can open PRs against it (branch protection optional for MVP).

---

## Checklist

- [x] Create folder `ops-config/` in this repo (subtree; no separate repo).
- [x] Add sample YAML: `alerts/thresholds.yaml`, `channels/channel_list.yaml`; self-contained, human-readable.
- [x] Document location and how agent/PR will target it: [ops-config/README.md](../../ops-config/README.md) (branch `main`, path `ops-config/`); main [README.md](../../README.md) updated.
- [x] If separate repo later: ops-config README describes split and auth (e.g. token for GitHub API).

---

## Test requirements

- [x] Repo/folder is in place; GitOps MCP (S2.2) can read and create branches/PRs against it (path `ops-config/`).
- [x] Sample config is valid and human-readable.
