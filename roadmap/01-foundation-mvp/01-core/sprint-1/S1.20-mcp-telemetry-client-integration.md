# S1.20 — MCP Telemetry client integration (citation-present)

| Field | Value |
|-------|--------|
| **Task ID** | S1.20 |
| **Status** | Done |

---

## Description

`citation-present` eval case still fails even when the Telemetry MCP server is running, because the agent’s MCP client (`call_telemetry`) returns an empty list for the time range and channel where fixture data exists. As a result, `investigate` sees no evidence, the run escalates, and S1.15’s stricter scoring correctly fails the case. This task fixes the Telemetry MCP client integration so that the agent actually receives telemetry samples and can produce grounded citations for `citation-present`.

---

## Requirements

- [x] **Fix Telemetry MCP client decoding:** Ensure `apps/agent/mcp_client.py::call_telemetry` correctly decodes `query_telemetry` results from the StreamableHTTP MCP server (using `structuredContent` or the correct content fields), so that for the `citation-present` window and `channels: ["bus_voltage"]` it returns at least one record.
- [x] **End-to-end evidence:** With Telemetry MCP running and fixtures in `data/telemetry/*.ndjson`, the `citation-present` eval run should produce non-empty `citations` and **not** escalate (normal report path).
- [x] **No regression for other tools:** KB MCP clients (`search_runbooks`, `search_postmortems`) continue to work or are updated with the same decoding pattern.

---

## Checklist

- [x] Inspect the MPC Python SDK tool result type (e.g. `structuredContent`, `content`, and any `.json` / `.data` payloads) and adapt `mcp_client._call_telemetry_mcp` to use the correct attribute(s) returned by `FastMCP` over StreamableHTTP.
- [x] Apply the same decoding pattern to `_call_kb_runbooks_mcp` and `_call_kb_postmortems_mcp` if needed, to keep behaviour consistent across all MCP tools.
- [x] Manually verify (local run): start Telemetry MCP (`python -m apps.mcp.telemetry_server.main`), then call `call_telemetry("2025-02-14T09:00:00Z", "2025-02-14T11:00:00Z, ["bus_voltage"])` and confirm it returns the expected fixture rows from `data/telemetry/telemetry.ndjson`.
- [x] Run `python -m evals.scoring` with Telemetry MCP running and confirm that the `citation-present` case passes (no escalation, citations present).

---

## Test requirements

- [ ] Unit or integration test for `call_telemetry` that exercises a real or mocked MCP server and asserts non-empty results for a window/channel that has fixture data.
- [x] Full eval run (`python -m evals.scoring`) passes all 8 cases when Telemetry MCP is running and fixtures are present (including `citation-present` with real citations).
- [ ] CI evals job (with Telemetry MCP started in the workflow) is green; `citation-present` no longer fails due to escalation when evidence is available.

