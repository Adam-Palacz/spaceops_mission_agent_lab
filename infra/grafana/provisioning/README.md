# Grafana provisioning

Auto-loads dashboards and datasources on Grafana startup (Compose profile).

Subfolders: **dashboards/** (JSON + `dashboards.yml`), **datasources/** (`prometheus.yml`).

Parent: [../README.md](../README.md).
