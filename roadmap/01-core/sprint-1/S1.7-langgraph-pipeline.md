# S1.7 — LangGraph: Triage → Investigate → Decide → Report

| Field | Value |
|-------|--------|
| **Task ID** | S1.7 |
| **Status** | Done |

---

## Description

Implement LangGraph pipeline in `apps/agent`: Triage (subsystem + risk) → Investigate (call Telemetry + KB MCPs, collect citations) → Decide (plan where each step must reference doc_id or snippet — NF5a) → Report (summary, evidence, actions, rollback, trace URL). No Act node yet. Goals: F2, F3, F4, F7; NF5a citation grounding.

---

## Requirements

- [x] Triage node: classify subsystem (ADCS/Power/Thermal/Comms/Payload/Ground) and risk; write incident record.
- [x] Investigate node: call Telemetry MCP and KB MCP; attach hypotheses and citations to state.
- [x] Decide node: produce plan (list of steps); each step must reference at least one doc_id or telemetry/snippet ID (citation grounding).
- [x] Report node: generate summary, evidence (snippets + citations), proposed actions, rollback, trace link (Jaeger URL).
- [x] State schema: incident_id, subsystem, risk, hypotheses, citations, plan, report.
- [x] Pipeline invocable from API/CLI trigger (S1.4).

---

## Checklist

- [x] Define state schema (e.g. TypedDict or Pydantic).
- [x] Implement Triage node (LLM or rule-based); persist incident record.
- [x] Implement Investigate node: call MCPs, aggregate citations.
- [x] Implement Decide node: LLM outputs plan; validate each step has doc_id/snippet ref.
- [x] Implement Report node: format summary, evidence, actions, rollback, trace link.
- [x] Wire graph: Triage → Investigate → Decide → Report; connect to run trigger.

---

## Test requirements

- End-to-end run on fixture incident produces report with trace link.
- Plan steps in output contain at least one citation (doc_id or snippet id).
- Triage output includes subsystem and risk; Investigate output includes hypotheses + citations.
