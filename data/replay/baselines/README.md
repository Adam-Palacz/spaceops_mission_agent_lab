# Replay baselines

Pinned JSON snapshots of replay scenarios (e.g. `ps35_burst_local.json`) used to detect
regressions without calling the live LLM on every check.

Update only when behavior changes intentionally. See [../README.md](../README.md).
